Step 1: Start Backend
Open CMD:

cd /d "Directory Name"
npm install
npm start

Wait until you see something like:

BarStock API → http://localhost:4000

Step 2: Start Frontend

Open a new CMD window:

cd /d Directory Name
npm install
npm run dev

Wait until you see:

Local: http://localhost:5173

Step 3: Open Browser

Open:

http://localhost:5173